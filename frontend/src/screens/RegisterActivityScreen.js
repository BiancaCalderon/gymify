import React from 'react';
import PlaceholderScreen from '../components/PlaceholderScreen';

// TODO (Sprint 3): selector de tipo de entrenamiento, duración, nota
// y preview de XP, conectado a POST /api/v1/activities
export default function RegisterActivityScreen() {
  return <PlaceholderScreen title="Registrar actividad" sprint={3} />;
}
