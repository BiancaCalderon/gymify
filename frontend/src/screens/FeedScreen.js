import React from 'react';
import PlaceholderScreen from '../components/PlaceholderScreen';

// TODO (Sprint 4): stories, posts, likes y comentarios,
// conectado a GET /api/v1/feed y POST /api/v1/feed/posts
export default function FeedScreen() {
  return <PlaceholderScreen title="Comunidad" sprint={4} />;
}
