import React from 'react';
import PlaceholderScreen from '../components/PlaceholderScreen';

// TODO (Sprint 2): formulario de registro (nombre, correo, contraseña)
// conectado a POST /api/v1/auth/register
export default function RegisterScreen() {
  return <PlaceholderScreen title="Registro de usuario" sprint={2} />;
}
