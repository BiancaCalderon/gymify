import React from 'react';
import PlaceholderScreen from '../components/PlaceholderScreen';

// TODO (Sprint 3-4): stats, logros desbloqueados y progreso de nivel,
// conectado a GET /api/v1/users/{id}
export default function ProfileScreen() {
  return <PlaceholderScreen title="Perfil" sprint={3} />;
}
